/**
 * Build order step 9: lesson flow (count-in -> chunk -> results), live pitch
 * indicator, per-note feedback.
 */
export { Lesson } from './Lesson';
export { useLesson } from './useLesson';
export type { LessonPhase, LessonState, UseLessonOptions } from './useLesson';
